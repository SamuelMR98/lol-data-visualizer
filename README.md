# lol-data-visualizer

A starter project for extracting League of Legends match telemetry (wards/kills) from Riot Match-V5 timelines and generating minimap heatmaps.

## Quickstart

### 1) Setup

```bash
python -m venv .venv
source .venv/bin/activate 
pip install -U pip
pip install -e.
```

